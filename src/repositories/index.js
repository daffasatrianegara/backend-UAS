const UsersRepo = require('./users.repositories')
const UserRepo = require('./profile-user.repositories')
const AdminRepo = require('./admin.repositories')
const PhotoRepo = require('./photo.repositories')
const TodoRepo = require('./todo.repositories')

module.exports = {
    UsersRepo,
    UserRepo,
    AdminRepo,
    PhotoRepo,
    TodoRepo
}